function print(text) {
    __print text;
}
function print_n_times(text, n) {
    // You can multiply a string by an int to repeat it
    print((text + "\n") * n);
}

let x = "this is a variable!!";
x = "Hello, World!";
print_n_times(x, 5);